我是一名正在學習 Java 的開發者，今天學到了「如何將資料庫存儲的扁平 CSV 字串轉換為群組化結構」以及「處理新舊版本資料相容性」的邏輯。

請幫我設計一個練習題，要求如下：

情境設計：設計一個「智慧病房監控系統」，紀錄病人的 3 組生命徵象（如：體溫、脈搏、血壓）。
資料格式：
舊格式 (Legacy)：字串長度為 3，每一位代表該組生命徵象的當前值（例如 "36.5,80,120"）。
新格式 (Detailed)：為了增加準確度，現在每一組要紀錄「連續 4 次」的測量值。所以字串長度變成了 12 筆（3組 × 4次，例如 "36.5,36.6,36.4,36.5,80,82,79,81..."）。
任務要求：
請提供一段原始字串資料。
要求我寫一段 Java 程式碼，先判斷它是舊格式還是新格式。
使用二維陣列 doubleGrid[3][4] 來接收資料。
如果是舊格式，要把唯一的那個值填入每一組的第 1 個欄位，其他補 0。
如果是新格式，要用迴圈和座標公式將 12 筆資料正確填入 [3][4] 的網格。
限制條件：
必須包含 split 的限制處理（處理空白欄位）。
必須包含陣列長度補足的保險機制（防止當機）。
請先給我題目描述、範例資料，等我寫完後我會再請你幫我 Code Review。